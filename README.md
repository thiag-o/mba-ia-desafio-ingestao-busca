# Desafio MBA Engenharia de Software com IA - Full Cycle

Descreva abaixo como executar a sua solução.


Para rodar é preciso ter o docker instalado, definir as variáveis de ambiente;

rodar o banco de dados com docker compose up

rodar o script ingest.py 

e finalmete rodar o script chat.py e abrirá o chat que você poderá perguntar tudo a respeito do documento


GOOGLE_API_KEY=
GOOGLE_EMBEDDING_MODEL=
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/rag
PG_VECTOR_COLLECTION_NAME=
PDF_PATH=